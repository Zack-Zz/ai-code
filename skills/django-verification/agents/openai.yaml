interface:
  display_name: "Django Verification"
  short_description: "Verification loop for Django projects: migrations, linting, tests with coverage, security scans, and deployment readin"
  brand_color: "#3B82F6"
  default_prompt: "Apply Django Verification guidance for this task"
policy:
  allow_implicit_invocation: true
