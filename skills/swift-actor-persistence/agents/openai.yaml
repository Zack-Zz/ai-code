interface:
  display_name: "Swift Actor Persistence"
  short_description: "Thread-safe data persistence in Swift using actors — in-memory cache with file-backed storage, eliminating data races "
  brand_color: "#3B82F6"
  default_prompt: "Apply Swift Actor Persistence guidance for this task"
policy:
  allow_implicit_invocation: true
